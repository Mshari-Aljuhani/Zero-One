$('.liked-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like-count').html('<%= @question.get_likes.size %> &128591');
    $(this).closet('.liked-btn').html('Like');
});

$('.like-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like-count').html('<%= @question.get_likes.size %> &128591');
    $(this).closet('.like-btn').html('Liked');
});